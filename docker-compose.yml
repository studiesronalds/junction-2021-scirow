version: '2'
services:
    laravel:
        image: shivergard/docker-webserver
        ports:
            - 5500:80
            - 5586:86
        volumes:
            - ./laravel:/var/www
    pandas:
        build: ./build/pandas
        volumes:
            - ./:/opt/storage
        command: tail -f /dev/null
